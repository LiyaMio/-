//其实是求不含1和0的素数但位数超过1e6，所以构造这样一个符合条件的数

int main()
{
	int t;
	cin >> t;
	while (t--)
	{
		int a;
		cin >> a;
		if (a == 1)
			cout << "-1" << endl;
		else
		{
			cout << "23";
			for (int i = 0; i < a - 2; ++i)
				cout << "3";
			cout << endl;
		}
	}
	return 0;
}
